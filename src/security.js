import {timingSafeEqual} from 'crypto'; import dns from 'dns/promises'; import net from 'net';
export const cors=(origins)=>(req,res,next)=>{const o=req.headers.origin;if(o&&origins.includes(o)){res.setHeader('Access-Control-Allow-Origin',o);res.setHeader('Vary','Origin');res.setHeader('Access-Control-Allow-Credentials','true');res.setHeader('Access-Control-Allow-Headers','Content-Type,X-CSRF-Token,X-Request-Id');res.setHeader('Access-Control-Allow-Methods','GET,POST,PATCH,DELETE,OPTIONS')}if(req.method==='OPTIONS')return res.sendStatus(204);next()};
export const csrf=(req,res,next)=>{if(!['POST','PUT','PATCH','DELETE'].includes(req.method)||req.path.startsWith('/api/auth/login')||req.path.startsWith('/api/auth/register'))return next();const cookie=req.cookies?.zoomcue_csrf, header=req.get('x-csrf-token');if(!cookie||!header||cookie.length!==header.length||!timingSafeEqual(Buffer.from(cookie),Buffer.from(header)))return res.status(403).json({error:'CSRF validation failed'});next()};
const privateIp=ip=>{if(net.isIPv4(ip)){const [a,b]=ip.split('.').map(Number);return a===10||a===127||a===0||(a===169&&b===254)||(a===172&&b>=16&&b<=31)||(a===192&&b===168)}return ip==='::1'||ip.startsWith('fc')||ip.startsWith('fd')||ip.startsWith('fe80')};
export async function assertSafeUrl(value){const u=new URL(value);if(!['http:','https:'].includes(u.protocol)||u.username||u.password)throw Error('unsafe_url');const ips=await dns.lookup(u.hostname,{all:true});if(!ips.length||ips.some(x=>privateIp(x.address)))throw Error('private_url');return u.toString()}
